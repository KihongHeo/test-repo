name: test

on:
  issues:
    types:
      - opened

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: my test
        uses: KihongHeo/test-action@v1.0
